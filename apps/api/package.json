{
  "name": "@planup/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "test": "TABLE_NAME=planup-test node --test --import tsx src/*.test.ts",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1114.0",
    "@aws-sdk/client-dynamodb": "^3.883.0",
    "@aws-sdk/lib-dynamodb": "^3.883.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22.18.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.23.12",
    "typescript": "^5.9.2"
  }
}
